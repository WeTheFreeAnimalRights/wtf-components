/**
 * Component used to DEFINE a multiple checkbox for the generated form
 */
export const StandardMultipleCheckbox = ({
    name,
    label,
    options,
    className,
    onChange,
    visible,
    ...props
}) => {
    return null;
};

StandardMultipleCheckbox.displayName = 'StandardMultipleCheckbox';
