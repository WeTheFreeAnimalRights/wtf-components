/**
 * Component used to DEFINE a toggle group for the generated form
 */
export const StandardToggleGroup = ({
    name,
    label,
    type,
    className,
    onChange,
    visible,
    ...props
}) => {
    return null;
};

StandardToggleGroup.displayName = 'StandardToggleGroup';
