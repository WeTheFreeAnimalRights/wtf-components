/**
 * Component used to DEFINE a time picker for the generated form
 */
export const StandardTimePicker = ({
    name,
    label,
    className,
    placeholder,
    onChange,
    visible,
    ...props
}) => {
    return null;
};

StandardTimePicker.displayName = 'StandardTimePicker';
