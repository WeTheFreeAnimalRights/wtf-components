/**
 * Component used to DEFINE a code input for the generated form
 */
export const StandardCodeInput = ({
    name,
    label,
    className,
    onChange,
    visible,
    ...props
}) => {
    return null;
};

StandardCodeInput.displayName = 'StandardCodeInput';
