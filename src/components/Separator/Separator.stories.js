import { Separator } from './index';

export default {
    title: 'Components/Separator',
    component: Separator,
    tags: ['autodocs'],
};

export const Primary = {
    args: {
        label: 'or',
    },
};
