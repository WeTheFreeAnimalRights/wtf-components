import { NumberInput } from './index';
import '../../base.css';

export default {
    title: 'Components/NumberInput',
    component: NumberInput,
    tags: ['autodocs'],
};

export const Primary = {
    args: {},
};
