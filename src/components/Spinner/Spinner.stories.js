import { Spinner } from './index';
import '../../base.css';

export default {
    title: 'Components/Spinner',
    component: Spinner,
    tags: ['autodocs'],
};

export const Primary = {
    args: {},
};
