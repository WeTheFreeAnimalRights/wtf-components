/**
 * Component used to DEFINE the columns of a datatable
 */
export const Column = ({
    name,
    type,
    sortable,
    label,
    className,
    headerClassName,
    defaultOrderBy,
    children,
    render,
    handleClick,
    ...props
}) => {
    return null;
};

Column.displayName = 'Column';
