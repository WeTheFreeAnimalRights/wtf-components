/**
 * Component used to DEFINE the columns of a datatable
 */
export const Column = ({
    name,
    type,
    serverName,
    sortable,
    label,
    className,
    defaultOrderBy,
    children,
    render,
    ...props
}) => {
    return null;
};

Column.displayName = 'Column';
